name: Trunkrs report check

on:
  schedule:
    # UTC covers 06:15/06:25 in both Dutch summer and winter time.
    # The other hour supplies two extra idempotent recovery checks.
    - cron: '15,25 4,5 * * *'
    # Temporary UTC probe: remove after a real schedule event has been verified.
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: trunkrs-report-check
  cancel-in-progress: false

jobs:
  request-sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Request signed GitHub identity and start report check
        env:
          AUDIENCE: https://planning.bbquality.nl/api/trunkrs/scheduled-sync
        run: |
          set -euo pipefail
          token_response=$(curl --fail --silent --show-error --max-time 15 \
            -H "Authorization: bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=${AUDIENCE}")
          identity_token=$(jq -er '.value' <<< "$token_response")
          curl --fail --silent --show-error --max-time 30 -X POST \
            -H "Authorization: Bearer ${identity_token}" \
            -H 'Accept: application/json' \
            "$AUDIENCE"
